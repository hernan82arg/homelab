apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: redis
  namespace: data
spec:
  interval: 5m
  chart:
    spec:
      chart: redis
      version: '8.0.10-23'
      sourceRef:
        kind: HelmRepository
        name: redis
        namespace: data
